(alias
 ((name runtest)
  (deps (test.t test.ml foo.ml))
  (action
    (progn
      (setenv MERLIN ${exe:../../merlin-wrapper}
      (setenv OCAMLC ${OCAMLC}
        (run ${bin:craml} ${<})))
      (diff? ${<} ${<}.corrected)))))


