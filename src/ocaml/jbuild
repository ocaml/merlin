(* -*- tuareg -*- *)

module J = Jbuild_plugin.V1
;;

Printf.ksprintf J.send {|
(copy_files# %s/*.ml*)

(library
 ((name typer)
  (wrapped false)
  (libraries (typing))))
|} (Scanf.sscanf J.ocaml_version "%s@.%s@." (fun maj min -> maj ^ min))
;;
