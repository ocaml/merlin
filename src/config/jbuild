(rule
 ((targets (my_config.ml))
  (mode fallback)
  (deps (gen_my_config.ml (universe)))
  (action (with-stdout-to ${@} (run ${OCAML} gen_my_config.ml ${ocaml_version})))))

(library
 ((name config)
  (wrapped false)
  (modules (my_config))))
